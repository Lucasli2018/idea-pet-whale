# Idea Pet Whale 升级路线图

> 基于 v0.0.1（约 1000 行 Java / 20 测试）的现状规划。
> 现有资产：9 动画精灵图状态机（`PetStateMachine`）、manifest v2 序列编排（`pet.json` 的 `sequences`）、
> 编辑器/VCS 监听、透明置顶窗（`PetFrame`）、两套主题、`decorations/whale` 装饰资源（尚未启用）。

## 当前进展（截至 v0.0.15）

路线图按「阶段 0.1.0」规划，实际按 0.0.x 补丁节奏快速迭代，以下能力已**提前落地**：

- **P0 全部**：设置页（卡片式 + 实时预览）、透明窗清屏防残影、尺寸/透明度 clamp、位置/隐藏状态持久化。
- **P1 大部分**：点击反馈（双击 happy / 30s 空闲 bored / 深夜 sleepy）、拖拽反应（waving + 台词）、
  气泡台词系统（`PetBubble` + `dialogue/whale.json`，渐变底 + 指向尾巴 + 淡入下滑动画，实时跟随宠物）。
- **P2 部分**：亲密度/等级/称号五阶、喂食（小鱼干消耗 +10 亲密度 +5 点数）、久坐关怀提醒、
  分区悬浮交互层（头顶数值胶囊 + 脚底按钮卡片）、"？"帮助说明弹窗（v0.0.15）。
- 待办：P2 心情系统（MoodState 双维度）、成就系统；P3 统计面板 / 自定义主题 / 台词包；P4 全部。


## 总览

| 阶段 | 版本 | 主题 | 核心交付 |
|---|---|---|---|
| P0 | 0.0.2 ~ 0.0.3 | 地基加固 | 设置页、Swing 稳定性、窗口记忆 |
| P1 | 0.1.0 | 交互 | 点击/抚摸反馈、右键菜单、气泡台词 |
| P2 | 0.2.0 | 情感陪伴 | 心情/亲密度、久坐关怀、成就庆祝 |
| P3 | 0.3.0 | 数据与个性化 | 统计面板、自定义主题/台词 |
| P4 | 0.4.0+ | 可选智能 | 外部事件桥、LLM 台词（默认关闭） |

---

## P0 地基加固（0.0.2 ~ 0.0.3）

交互和情感都建立在"稳定可配置"之上，先补课。

1. **设置页**（`PersistentStateComponent` + `Configurable`）
   - 新增 `PetSettingsState`：窗口大小、初始位置、不透明度、默认主题。
   - `PetFrame` 启动时读取，替代当前写死的尺寸/位置。
2. **Swing 稳定性**（直接复用 idea-pet 的踩坑结论）
   - `PetPanel.paintComponent` 在 `super` 后 `clearRect` 每帧清屏，防精灵图残影。
   - 尺寸 clamp（建议 60~300），防止配置越界。
   - WebP 解码加 LRU 有界缓存，避免反复 IO。
3. **窗口记忆**：位置/大小/隐藏状态持久化，重启还原。
4. **装饰资源启用**：`decorations/whale/decoration.json` 已在包里，接一个"气泡/周边装饰"渲染通道备用。

验收：`mvn clean verify` 全绿 + 测试数 ≥ 28；重启后位置记忆生效。

---

## P1 交互功能（0.1.0）

让鲸鱼娘"能被摸"。

1. **点击反馈**
   - 单击 → 触发 `waving`（2 段随机台词气泡）。
   - 连续点击（摸头，1 秒内 ≥3 次）→ `jumping` + 开心台词；点太多次（>6 次）→ `failed` 撒娇"讨厌啦"。
   - 实现点：`PetPanel` 已有 mouse 事件（拖拽用），在 `MouseListener` 上区分 click/drag，转发 `PetStateService`。
2. **右键菜单**（`JPopupMenu`，替代/扩展现有悬停面板）
   - 隐藏 / 召唤、切换主题、投喂小鱼干、设置入口、心情查看（为 P2 预留）。
3. **气泡台词系统**（情感表达的核心载体）
   - 新增 `ui/PetBubble.java`：跟随窗体的圆角气泡，2.5s 自动淡出，绝不遮挡动画本体。
   - 台词库：`resources/dialogue/whale.json`，按事件分组（`click` / `done` / `failed` / `greet` / `idle-long`…），解析直接用平台捆绑的 Gson（不再手写 parser）。
   - 事件挂点：启动问候、测试全绿（done）、构建失败（failed）、久不写代码（idle-long）。
4. **拖拽反应**：拖动中切 `running`，放下后随机 `waving`/`waiting` + 台词。

验收：四种事件（点击/摸头/投喂/事件台词）均可触发且互不打断拖拽；测试 ≥ 40。

---

## P2 情感陪伴（0.2.0）★ 本路线图核心

让鲸鱼娘从"状态显示器"升级为"有心情的伙伴"。设计上是**在 ActivityPhase 与 Animation 之间插一层 MoodState**，不动现有状态机契约。

1. **心情系统（MoodState）**
   - 两个维度：`happiness`（0~100，随互动/成功事件上升、失败事件下降）与 `energy`（随连续编码时长下降、休息恢复）。
   - 心情影响动画编排：同是 idle，心情高走 `idle→waving` 序列，心情低走 `waiting→failed` 序列——通过运行时改写 `sequences` 选择实现，无需新素材。
   - 心情持久化（关闭 IDE 也保存，回来时按间隔时间恢复"她记得你"）。
2. **亲密度（Affection Level）**
   - 累计互动（点击/投喂/陪伴时长）涨经验，Lv.1→Lv.5。
   - 等级解锁：Lv.2 更多台词、Lv.3 称呼变化（"你好"→"今天也辛苦啦"）、Lv.5 悬停面板显示专属徽章。
3. **久坐关怀（程序员健康向）**
   - 连续编码 60min → 气泡提醒喝水/起身（可在设置关）；深夜（23 点后）→ 困倦台词 + `waiting` 频率上升。
   - 实现：`PetStateService` 内 `ScheduledExecutor` + 编辑器活跃时间统计。
4. **成就系统**
   - 触发点：单日测试全绿 ≥10 次、连续 7 天陪伴、首次拖到屏幕正中央……
   - 表现：专属庆祝序列 + 气泡 + 悬停面板成就列表（` JPanel` 列表即可，不引入新 UI 框架）。

验收：心情/亲密度跨重启持久；关怀提醒可关闭；所有情感状态可从悬停面板查看。

---

## P3 数据与个性化（0.3.0）

1. **陪伴统计面板**：今日编码时长、测试通过率、与鲸鱼娘互动次数、亲密度曲线（可参考 `idea-keyboard-stats` 的统计与展示套路）。
2. **自定义主题**：利用 manifest v2 的相对路径解析，支持用户导入 `pet.json + spritesheet` 目录（"宠物商店"式选择器），第三方素材零代码接入。
3. **台词自定义**：`dialogue/whale.json` 支持外部文件覆盖（IDE config 目录优先），用户可写自己的台词包。
4. **多宠物共存位**：manifest 驱动的宠物注册表，为后续接入 dsh-pet 其他角色铺路。

---

## P4 可选智能（0.4.0+，全部默认关闭）

1. **外部事件桥**：状态机已与 dsh-pet `state.ts` 对齐，可加一个本地 HTTP/文件事件入口，让 CI 结果、外部脚本也能驱动鲸鱼娘。
2. **LLM 动态台词**：接用户自配的 API 生成个性化陪伴文案。**隐私红线：默认关闭、显式开关、无 key 不请求。** HTTP 优先走 JDK 自带 `HttpClient`，无需为此新增依赖。

---

## 技术红线（所有阶段通用）

- **依赖引入原则（宽松版）**：默认仍以 JDK + IntelliJ Platform SDK 为主；**为了优化体验，可以适当引入第三方依赖**，但须遵守三条约束：
  1. **能用平台自带的就不新增**：IDEA 平台已捆绑 Gson（`com.google.gson`）、kotlinx 等库，解析 JSON、做序列化优先直接用，不重复打包；
  2. **引入的库必须"小而稳"**：单用途、活跃维护、体积小（如 Gson、Commons-CLI 这类），不引入大型框架（Spring/JavaFX/ffmpeg 仍然禁止）；新增依赖须在 ROADMAP 对应阶段注明用途与理由；
  3. **插件 zip 体积敏感**：桌宠是常驻小插件，单次引入的依赖总体积控制在 1~2MB 以内，超出先讨论。
  - 现有手写 `PetManifestParser` 不强制重写，可在引入 Gson 后逐步迁移。
- **JDK 17 / since-build 231**：不用 JCEF、不碰 JavaFX/ffmpeg；新 API 必须是 2023.1 就有的。
- **UI 三不原则**：气泡不遮挡动画、面板元素不重叠（flex-column + gap 自然堆叠）、每帧显式清屏。
- **测试同步增长**：每个新状态/持久化字段都有对应用例，目标是每阶段发布时 `mvn clean verify` 全绿。
- 版本号沿用 v0.0.1 起每阶段 +0.1 的节奏，`change-notes` 同步更新。
